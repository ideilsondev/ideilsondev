import type { Metadata } from "next";
import Link from "next/link";
import { ArrowLeft, ArrowUpRight, Bell, CheckCircle2, CircleAlert, MoreHorizontal } from "lucide-react";
import { demos, isDemoSlug } from "@/lib/demos";
import { notFound } from "next/navigation";

export function generateStaticParams(){return Object.keys(demos).map(slug=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;if(!isDemoSlug(slug))return {};const demo=demos[slug];return {title:`${demo.name} — demonstração`,description:demo.summary,alternates:{canonical:`/demos/${slug}`}}}
export default async function DemoPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!isDemoSlug(slug))notFound();const demo=demos[slug];const accent=demo.accent==="emerald"?"text-emerald-300 bg-emerald-300/10 border-emerald-300/20":demo.accent==="sky"?"text-sky-300 bg-sky-300/10 border-sky-300/20":"text-amber-300 bg-amber-300/10 border-amber-300/20";return <main className="min-h-screen bg-[var(--canvas)]"><header className="border-b border-white/10"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 md:px-12"><Link href="/" className="inline-flex items-center gap-2 text-sm font-medium text-[var(--muted)] hover:text-white"><ArrowLeft size={17} aria-hidden="true"/>Voltar ao portfólio</Link><p className="font-mono text-xs font-semibold uppercase tracking-[.12em] text-[var(--faint)]">Interface demonstrativa</p></div></header><section className="mx-auto max-w-7xl px-6 py-16 md:px-12 lg:py-24"><p className="eyebrow">{demo.eyebrow}</p><div className="mt-5 grid gap-12 lg:grid-cols-[.82fr_1.18fr] lg:items-end"><div><h1 className="max-w-xl text-4xl font-semibold tracking-[-.055em] text-white sm:text-5xl">{demo.title}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-[var(--muted)]">{demo.summary}</p><p className="mt-6 font-mono text-xs font-semibold uppercase tracking-[.08em] text-emerald-200">{demo.pill}</p><Link href="#contact" className="mt-9 inline-flex items-center gap-2 font-semibold text-emerald-300 hover:text-emerald-200">Quero uma aplicação assim <ArrowUpRight size={17} aria-hidden="true"/></Link></div><section aria-label={`Prévia da aplicação ${demo.name}`} className="overflow-hidden rounded-3xl border border-white/10 bg-[#0a1726] shadow-2xl shadow-black/20"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div><p className="text-sm font-semibold text-white">{demo.name}</p><p className="mt-1 font-mono text-[.65rem] uppercase tracking-[.1em] text-[var(--faint)]">ambiente demonstrativo</p></div><button type="button" aria-label="Mais opções" className="rounded-lg p-2 text-[var(--muted)] hover:bg-white/5 hover:text-white"><MoreHorizontal size={19} aria-hidden="true"/></button></div><div className="grid gap-4 p-5 sm:grid-cols-[1.1fr_.9fr]"><div className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><p className="font-mono text-[.65rem] font-bold uppercase tracking-[.1em] text-[var(--faint)]">{demo.metricLabel}</p><p className="mt-3 text-4xl font-semibold tracking-[-.05em] text-white">{demo.metric}</p><div className="mt-7 flex h-16 items-end gap-1.5" aria-label="Gráfico ilustrativo"><span className="h-[35%] flex-1 rounded-t bg-white/10"/><span className="h-[58%] flex-1 rounded-t bg-white/15"/><span className="h-[46%] flex-1 rounded-t bg-white/15"/><span className="h-[80%] flex-1 rounded-t bg-emerald-300/70"/><span className="h-[68%] flex-1 rounded-t bg-emerald-300"/><span className="h-[92%] flex-1 rounded-t bg-emerald-200"/></div></div><div className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><p className="font-mono text-[.65rem] font-bold uppercase tracking-[.1em] text-[var(--faint)]">Estado agora</p><div className={`mt-4 rounded-xl border p-3 ${accent}`}><div className="flex gap-2 text-sm font-semibold"><CheckCircle2 size={17} aria-hidden="true"/>{demo.primary}</div></div><div className="mt-3 flex items-center gap-2 text-sm text-[var(--muted)]"><Bell size={16} aria-hidden="true"/>{demo.secondary}</div></div><div className="sm:col-span-2 rounded-2xl border border-white/10 bg-white/[.035] p-2">{demo.items.map((item,index)=><div key={item} className="flex items-center justify-between gap-3 rounded-xl px-3 py-3 text-sm"><div className="flex items-center gap-3"><span className={`grid h-7 w-7 place-items-center rounded-lg text-xs font-bold ${index===2?"bg-amber-300/10 text-amber-300":"bg-emerald-300/10 text-emerald-300"}`}>{index+1}</span><span className="text-slate-200">{item}</span></div>{index===2?<CircleAlert size={17} className="text-amber-300" aria-label="Atenção"/>:<CheckCircle2 size={17} className="text-emerald-300" aria-label="Concluído"/>}</div>)}</div></div></section></div></section><section id="contact" className="border-t border-white/10 px-6 py-12 text-center"><p className="text-[var(--muted)]">Esta interface usa dados fictícios para demonstrar uma direção de produto.</p><Link href="/#contact" className="mt-5 inline-flex rounded-full bg-emerald-300 px-5 py-3 font-semibold text-slate-950 hover:bg-emerald-200">Conversar sobre seu projeto</Link></section></main>}
